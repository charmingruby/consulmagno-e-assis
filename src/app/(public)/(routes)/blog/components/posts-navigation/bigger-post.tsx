interface BiggerPostProps {
  coverPhoto: string
  title: string
  subtitle: string
  slug: string
  publishedDate: Date
}

export function BiggerPost({
  coverPhoto,
  title,
  subtitle,
  slug,
  publishedDate,
}: BiggerPostProps) {
  return <>oi</>
}
