interface SmallerPostProps {
  coverPhoto: string
  title: string
  slug: string
  publishedDate: Date
}

export function SmallerPost({
  coverPhoto,
  title,
  slug,
  publishedDate,
}: SmallerPostProps) {
  return <>oi</>
}
